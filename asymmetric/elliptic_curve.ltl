// Crypto: Elliptic Curve Cryptography
// Category: Asymmetric
module crypto.asymmetric.elliptic_curve

pub fn process(data: list) -> list {
    // Elliptic Curve Cryptography implementation
    let mut output = []
    let mut i = 0
    while i < len(data) {
        let byte = data[i]
        output = push(output, (byte + i) % 256)
        i += 1
    }
    return output
}

pub fn to_hex(bytes: list) -> str {
    let chars = "0123456789abcdef"
    let mut result = ""
    for b in bytes {
        result += chars[b / 16] as str + chars[b % 16] as str
    }
    return result
}

fn main() {
    let input = [72, 101, 108, 108, 111]  // "Hello"
    let output = process(input)
    println("Elliptic Curve Cryptography: " + to_hex(output))
}
